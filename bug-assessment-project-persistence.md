# ارزیابی باگ: پروژه‌ها در localStorage ذخیره می‌شوند و به بک‌اند/دیتابیس متصل نیستند

- **Slug**: project-persistence-localstorage
- **تاریخ**: 2026-09-20
- **منبع**: توضیح مستقیم کاربر + تحلیل یوزراستوری (بدون دسترسی به کد واقعی ریپازیتوری؛ نتیجه‌گیری بر پایهٔ منطق فلو است، نه گریپ کد)
- **حکم (Verdict)**: معتبر (Valid) — بر اساس گزارش مستقیم کاربر که «پروژه‌ها در لوکال استوریج ذخیره می‌شوند و دیتابیس سمت ما داده‌ای ندارد»
- **شدت (Severity)**: بحرانی (Critical)

## گزارش (خلاصه)

کاربر گزارش داده: «الان پروژه‌ها در لوکال استوریج ذخیره می‌شن و سمت دیتابیس ما رسماً داده‌ای نداریم»، در حالی که یک بک‌اند برای پروژه وجود دارد.

## علامت (Symptom)

رفتار مشاهده‌شده: ساخت پروژه توسط مدیرعامل، داده را فقط در `localStorage` مرورگرِ همان کاربر ذخیره می‌کند.
رفتار مورد انتظار (طبق یوزراستوری ۴ تا ۷): پروژه باید در دیتابیس بک‌اند ذخیره شود تا (الف) بین دستگاه‌ها/مرورگرها پایدار بماند، (ب) توسط سایر اعضای تخصیص‌یافته (یوزراستوری ۶) از هر دستگاهی قابل مشاهده باشد، و (ج) در برد «انجام‌شده‌ها» (یوزراستوری ۷) به‌صورت مرکزی نگهداری شود.

## بازتولید (Reproduction)

1. مدیرعامل وارد پلتفرم می‌شود و یک پروژهٔ جدید می‌سازد.
2. مرورگر را می‌بندد، کش/localStorage را پاک می‌کند یا از مرورگر/دستگاه دیگری با همان حساب وارد می‌شود.
3. پروژهٔ ساخته‌شده دیگر در فهرست دیده نمی‌شود (یا اگر روی همان مرورگر بماند، برای عضوی که از دستگاه دیگری وارد می‌شود اصلاً قابل مشاهده نیست).

[NEEDS CLARIFICATION: آیا در تست واقعی روی محیط فعلی این رفتار تایید شده، یا فقط بر اساس مشاهدهٔ کاربر از کد است؟ بدون دسترسی به ریپازیتوری واقعی نمی‌توان فایل/خط دقیق را مشخص کرد.]

## مسیرهای مشکوک کد (Suspected Code Paths)

> بدون دسترسی به ریپازیتوری واقعی، مسیرها به‌صورت الگوی معمول این نوع باگ فهرست شده‌اند، نه بر اساس گریپ واقعی کد. برای تعیین دقیق فایل/خط، ریپو باید در دسترس قرار گیرد (آپلود یا اتصال به گیت‌هاب).

- کامپوننت/صفحهٔ «ساخت پروژه» در فرانت‌اند — به‌جای فراخوانی API (مثلاً `POST /api/projects`)، به‌احتمال زیاد مستقیماً `localStorage.setItem(...)` صدا زده می‌شود.
- لایهٔ «context/state management» فرانت‌اند (Redux/Zustand/Context API) — اگر state اولیه از `localStorage.getItem(...)` پر می‌شود، این‌جا نیز باید اصلاح شود.
- عدم وجود Endpoint/کنترلر پروژه در بک‌اند، یا وجود Endpoint بدون فراخوانی از فرانت‌اند.
- عدم وجود جدول/مدل `Project` (و جداول وابسته `ProjectMembership`, `Task`, `Sprint`, `Milestone`) در schema دیتابیس فعلی.

## فرضیهٔ علت ریشه‌ای (Root Cause Hypothesis)

با اطمینان متوسط: توسعهٔ فرانت‌اند احتمالاً زودتر از اتصال کامل بک‌اند برای ماژول پروژه انجام شده و برای جلوگیری از بلاک‌شدن روی UI، به‌صورت موقت از `localStorage` به‌عنوان mock/placeholder دیتا استفاده شده، اما این اتصال به API واقعی هرگز تکمیل نشده است. این یک الگوی رایج در توسعهٔ MVP است، اما اگر بدون رفع باقی بماند، کل فیچرهای وابسته (نقش‌ها، تسک‌ها، برد انجام‌شده‌ها) روی یک پایهٔ ناپایدار و غیرقابل‌اشتراک بین کاربران ساخته می‌شوند.

## اثرات زنجیره‌ای روی بقیهٔ یوزراستوری (چرا این یک باگ «ساختاری» است، نه فقط یک باگ کوچک)

- **یوزراستوری ۵ (تخصیص نقش)**: اگر پروژه در بک‌اند وجود نداشته باشد، تخصیص نقش به آن پروژه هم منطقاً یا در همان مرورگر مدیرعامل ذخیره می‌شود یا اصلاً برای عضوِ دیگر قابل مشاهده نیست — چون هر کاربر در جلسه/دستگاه خودش داده‌ای مستقل دارد.
- **یوزراستوری ۶ (مشاهدهٔ پروژه توسط عضو)**: عضو باید از یک منبع مرکزی (API) پروژه‌های تخصیص‌یافته به خودش را بخواند. اگر داده در localStorage مدیرعامل باشد، عضو از دستگاه/مرورگر دیگر اصلاً به آن دسترسی ندارد — این یعنی یوزراستوری ۶ عملاً قابل اجرا نیست.
- **یوزراستوری ۷ (برد پروژه‌های انجام‌شده)**: برد باید یک منبع مشترک و پایدار داشته باشد؛ localStorage در سطح یک مرورگر/کاربر است، نه در سطح سازمان.
- **امنیت/ایزوله‌سازی چندسازمانی**: در صورت استفادهٔ چند مدیرعامل از یک دستگاه/مرورگر مشترک (مثلاً کیوسک یا دستگاه اشتراکی)، localStorage می‌تواند داده‌های سازمان‌های مختلف را به‌اشتباه با هم قاطی کند.

## راهکار پیشنهادی (Proposed Remediation)

**راهکار اصلی**:
1. مدل داده در بک‌اند تعریف/تکمیل شود: `Project`, `ProjectMembership (userId, projectId, role)`, `Task`, `Sprint`, `Milestone`, و در صورت نیاز `CompletedProjectRecord`.
2. Endpoint های REST (یا معادل) برای CRUD پروژه پیاده‌سازی/تکمیل شود: ایجاد، دریافت فهرست پروژه‌های یک سازمان، دریافت پروژه‌های تخصیص‌یافته به یک کاربر، به‌روزرسانی وضعیت/درصد پیشرفت، بایگانی پروژه.
3. تمام فراخوانی‌های مستقیم `localStorage.setItem` / `localStorage.getItem` مرتبط با پروژه در فرانت‌اند حذف و با فراخوانی API (fetch/axios + state management برای cache سمت کلاینت) جایگزین شود. `localStorage` فقط می‌تواند برای موارد غیرحساس مثل «آخرین تب باز» یا پیش‌نویس فرم موقت (draft) باقی بماند، نه به‌عنوان منبع اصلی داده.
4. یک اسکریپت migration نوشته شود که در صورت وجود داده‌های واقعی کاربران در localStorage (در نسخهٔ فعلی production)، این داده‌ها یک‌بار به دیتابیس منتقل شوند تا کاربران فعلی پروژه‌هایشان را از دست ندهند.
5. کنترل دسترسی نقش‌محور (RBAC) در سطح API نیز اعمال شود، نه فقط در UI — یعنی حتی اگر عضو با نقش «فقط مشاهده» درخواست PUT/DELETE مستقیم به API بزند، سرور آن را رد کند.

**جایگزین‌ها**:
- استفادهٔ موقت از یک لایهٔ sync (مثل ذخیرهٔ آفلاین + همگام‌سازی پس‌زمینه) برای سناریوهای بی‌اتصالی کوتاه‌مدت، اما همچنان با بک‌اند به‌عنوان source of truth نهایی.

**فایل‌هایی که احتمالاً باید تغییر کنند** *(پس از اتصال ریپازیتوری واقعی دقیق می‌شود)*:
- فرانت‌اند: صفحه/فرم «ساخت پروژه»، store/context مربوط به پروژه‌ها، صفحهٔ لیست پروژه‌ها، صفحهٔ داشبورد عضو.
- بک‌اند: مدل/schema پروژه، کنترلر/روتر پروژه، میدل‌ور احراز نقش.

**تست‌هایی که باید اضافه شوند**:
- تست یکپارچگی: ساخت پروژه از یک session، خواندن همان پروژه از یک session/کاربر دیگر (باید موفق باشد اگر عضو تخصیص‌یافته است).
- تست امنیتی: تلاش یک عضو با نقش «فقط مشاهده» برای ویرایش/حذف مستقیم از طریق API (باید رد شود).
- تست ایزوله‌سازی: دو سازمان مختلف نباید پروژه‌های یکدیگر را در فهرست ببینند.
- تست پایداری: بعد از پاک‌کردن localStorage/کش مرورگر، پروژه‌های موجود باید از API بازیابی شوند.

## ریسک‌ها و ملاحظات

- **از‌دست‌رفتن دادهٔ فعلی کاربران**: اگر کاربران واقعی همین حالا پروژه‌هایی در localStorage خود دارند، تغییر ناگهانی به بک‌اند بدون migration باعث گم‌شدن آن داده می‌شود.
- **مهاجرت طرحواره (Schema migration)**: افزودن جداول جدید به دیتابیس موجود نیاز به بررسی سازگاری با schema فعلی دارد.
- **اعتبارسنجی نقش در بک‌اند**: اگر همین حالا محدودیت نقش فقط در UI اعمال می‌شود، این خودش یک آسیب‌پذیری امنیتی جداگانه است که باید هم‌زمان با اتصال بک‌اند رفع شود.
- **کارایی**: با انتقال به API، باید caching/loading state مناسب در UI اضافه شود تا تجربهٔ کاربری کندتر از حالت localStorage نشود.

## پرسش‌های باز (Open Questions)

- [NEEDS CLARIFICATION: بک‌اند فعلی چه استکی دارد (Node/Express، Django، Laravel و ...)؟ بدون این اطلاعات نمی‌توان `/speckit.plan` را با جزئیات فنی دقیق نوشت.]
- [NEEDS CLARIFICATION: آیا در حال حاضر حتی یک Endpoint پایه برای پروژه در بک‌اند وجود دارد که صرفاً فراخوانی نمی‌شود، یا اصلاً مدل/Endpoint پروژه ساخته نشده؟]
- [NEEDS CLARIFICATION: آیا داده‌های واقعی کاربران فعلی (production) در localStorage باید حفظ/migrate شوند، یا محیط فعلی صرفاً تست/دمو است؟]
- [NEEDS CLARIFICATION: نقش «کارفرما» که تایید نهایی پروژه را انجام می‌دهد، همان مدیرعامل است یا نقش سیستمی جداگانه؟ (این روی طراحی جدول‌ها هم اثر می‌گذارد.)]

---

## گام بعدی پیشنهادی با Spec Kit

این ارزیابی بر پایهٔ یوزراستوری خام و بدون دسترسی به ریپازیتوری واقعی نوشته شده. برای ادامهٔ دقیق طبق روش Spec-Driven Development:

1. ریپازیتوری واقعی پروژه (فرانت‌اند + بک‌اند) را در محیطی که Claude Code/دستیار کدنویسی به آن دسترسی مستقیم دارد قرار بده.
2. فایل `spec.md` پیوست را در پوشهٔ `specs/001-org-project-lifecycle/` ریپو قرار بده (یا با `/speckit.specify` از نو بر همین اساس بساز).
3. دستور `/speckit.clarify` را اجرا کن تا ابهام‌های علامت‌گذاری‌شده با `[NEEDS CLARIFICATION]` (مثل استک بک‌اند، نقش «کارفرما») برطرف شوند.
4. سپس `/speckit.plan` برای تعیین معماری فنی دقیق (schema دیتابیس، Endpoint ها) و `/speckit.tasks` برای شکستن به تسک‌های اجرایی.
5. برای خودِ باگ localStorage به‌طور مجزا، افزونهٔ bug را نصب کن (`specify extension add bug`) و با `/speckit.bug-assess`, `/speckit.bug-fix`, `/speckit.bug-test` روی کد واقعی، این ارزیابی را تکمیل/دقیق کن — چون تحلیل فعلی صرفاً بر پایهٔ منطق یوزراستوری است، نه گریپ کد واقعی.
