# مشخصات فیچر: مدیریت سازمان، اعضا و پروژه‌ها (Onboarding → Project Lifecycle)

**شاخه فیچر**: `001-org-project-lifecycle`

**تاریخ ایجاد**: 2026-09-20

**وضعیت**: پیش‌نویس (Draft) — بر اساس یوزراستوری خام کاربر، آماده برای `/speckit.clarify` و `/speckit.plan`

**ورودی**: یوزراستوری داده‌شده توسط کاربر دربارهٔ ثبت‌نام مدیرعامل، دعوت اعضا، ساخت پروژه، تخصیص نقش، اجرای تسک/اسپرینت و آرشیو پروژه‌های تکمیل‌شده

---

## سناریوهای کاربر و تست *(الزامی)*

> هر یوزراستوری باید مستقل قابل توسعه، تست، و دمو باشد. اولویت‌ها بر اساس اینکه بدون کدام‌یک، MVP بی‌معنی می‌شود تعیین شده‌اند.

### یوزراستوری ۱ — ثبت‌نام مدیرعامل با احراز کد ملی (اولویت: P1)

مدیرعامل از طریق یک لینک اختصاصی (invite/registration link) وارد می‌شود، اطلاعات حساب و کد ملی خود را وارد می‌کند. کد ملی باید توسط پلتفرم/تیم پشتیبانی تایید شود پیش از فعال شدن کامل حساب.

**چرا این اولویت**: بدون این مرحله هیچ سازمانی، پروژه‌ای یا عضوی نمی‌تواند در سیستم وجود داشته باشد؛ نقطهٔ ورود کل سیستم است.

**تست مستقل**: می‌توان با ثبت‌نام یک مدیرعامل جدید و بررسی وضعیت "در انتظار تایید" ↔ "تایید‌شده" این استوری را به‌طور کامل و مستقل تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** لینک ثبت‌نام معتبر، **وقتی** مدیرعامل فرم را با کد ملی صحیح پر می‌کند، **آنگاه** حساب در وضعیت «در انتظار تایید» ساخته می‌شود و امکان ورود کامل به داشبورد وجود ندارد.
2. **با فرض** حساب در وضعیت «در انتظار تایید»، **وقتی** تیم پلتفرم کد ملی را تایید می‌کند، **آنگاه** حساب به «فعال» تغییر وضعیت می‌دهد و مدیرعامل ایمیل/پیامک اطلاع‌رسانی دریافت می‌کند.
3. **با فرض** کد ملی نامعتبر یا تکراری (قبلاً برای حساب دیگری ثبت شده)، **وقتی** فرم ارسال می‌شود، **آنگاه** سیستم خطای مشخص نمایش می‌دهد و حساب دوم ساخته نمی‌شود.

---

### یوزراستوری ۲ — دعوت/افزودن اعضا با کد سازمانی (اولویت: P1)

مدیرعامل، پس از فعال شدن حساب، یک «کد سازمانی» (organization/personal code) دریافت می‌کند و آن را به افراد سازمان می‌دهد تا از طریق آن به پلتفرم بپیوندند.

**چرا این اولویت**: بدون اعضا، تخصیص نقش و پروژه بی‌معناست؛ این استوری پایهٔ چندنفره‌بودن سیستم است.

**تست مستقل**: می‌توان با تولید کد سازمانی و ثبت‌نام یک کاربر آزمایشی با همان کد، بدون نیاز به پروژه یا تسک، این استوری را تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** مدیرعامل حساب فعال دارد، **وقتی** وارد بخش «اعضا» می‌شود، **آنگاه** یک کد سازمانی یکتا برایش نمایش داده می‌شود (قابل بازتولید/باطل‌کردن).
2. **با فرض** کد سازمانی معتبر است، **وقتی** یک کاربر جدید هنگام ثبت‌نام آن را وارد می‌کند، **آنگاه** حساب او به‌عنوان زیرمجموعهٔ همان مدیرعامل ثبت می‌شود.
3. **با فرض** کد سازمانی باطل شده یا منقضی شده، **وقتی** کاربری با آن کد ثبت‌نام می‌کند، **آنگاه** ثبت‌نام رد می‌شود.

---

### یوزراستوری ۳ — ورود اعضا به‌عنوان زیرمجموعهٔ مدیرعامل (اولویت: P2)

افراد دعوت‌شده حساب می‌سازند/وارد می‌شوند و در سلسله‌مراتب سازمانی زیر مدیرعامل مربوطه قرار می‌گیرند، اما تا پیش از تخصیص به پروژه، دسترسی محتوایی خاصی ندارند.

**چرا این اولویت**: نتیجهٔ مستقیم استوری ۲ است، اما ارزش کسب‌وکاری‌اش با تخصیص پروژه (استوری ۵) کامل می‌شود.

**تست مستقل**: می‌توان با ورود یک عضو تازه‌ثبت‌شده و بررسی این‌که او هیچ پروژه‌ای نمی‌بیند (چون هنوز assign نشده)، این استوری را تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** عضو با کد سازمانی صحیح ثبت‌نام کرده، **وقتی** وارد داشبورد می‌شود، **آنگاه** به‌عنوان زیرمجموعهٔ مدیرعامل صحیح در فهرست اعضا نمایش داده می‌شود.
2. **با فرض** عضو هنوز به هیچ پروژه‌ای اضافه نشده، **وقتی** وارد داشبورد پروژه‌ها می‌شود، **آنگاه** حالت خالی («پروژه‌ای نداری») نمایش داده می‌شود، نه خطا و نه پروژه‌های سایر سازمان‌ها.

---

### یوزراستوری ۴ — ایجاد پروژه توسط مدیرعامل (اولویت: P1)

مدیرعامل پروژه‌ای جدید ایجاد می‌کند؛ با ایجاد پروژه، قابلیت‌هایی (تسک/اسپرینت/مایلستون/تخصیص نقش) برای آن پروژه فعال می‌شود.

**چرا این اولویت**: پروژه، واحد اصلی ارزش کل پلتفرم است. **این استوری محل باگ گزارش‌شدهٔ فعلی است**: پروژه‌ها در حال حاضر در localStorage مرورگر ذخیره می‌شوند، نه در بک‌اند/دیتابیس.

**تست مستقل**: می‌توان با ساخت یک پروژه، رفرش صفحه، پاک‌کردن کش مرورگر، و ورود از دستگاه دیگر، تست کرد که پروژه هنوز وجود دارد (یعنی از بک‌اند خوانده می‌شود، نه از localStorage).

**سناریوهای پذیرش**:

1. **با فرض** مدیرعامل فرم ساخت پروژه را تکمیل می‌کند، **وقتی** دکمهٔ «ایجاد پروژه» را می‌زند، **آنگاه** یک درخواست به API بک‌اند ارسال می‌شود و پروژه در دیتابیس ذخیره می‌شود (نه فقط در localStorage مرورگر).
2. **با فرض** پروژه ساخته شده، **وقتی** مدیرعامل کش مرورگر را پاک می‌کند یا از دستگاه/مرورگر دیگری وارد می‌شود، **آنگاه** پروژه همچنان در فهرست پروژه‌ها وجود دارد.
3. **با فرض** دو مدیرعامل مختلف در دو سازمان مختلف، **وقتی** هرکدام پروژه می‌سازند، **آنگاه** هیچ‌کدام پروژهٔ سازمان دیگری را نمی‌بینند (ایزوله‌سازی چندمستأجری/multi-tenant).

---

### یوزراستوری ۵ — تخصیص نقش افراد در پروژه (اولویت: P2)

مدیرعامل بر اساس نقش (مثلاً توسعه‌دهنده، طراح، ادمین پروژه، ناظر) افراد را به هر پروژه اضافه می‌کند.

**چرا این اولویت**: پیش‌نیاز مشاهدهٔ پروژه توسط اعضا (استوری ۶) و کنترل دسترسی است.

**تست مستقل**: می‌توان با اضافه‌کردن یک عضو به یک پروژه با نقش مشخص و بررسی محدودیت‌های آن نقش (مثلاً ویرایش تسک در مقابل فقط مشاهده)، این استوری را مستقل تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** پروژه و عضو هر دو موجودند، **وقتی** مدیرعامل عضو را با نقش مشخص به پروژه اضافه می‌کند، **آنگاه** این تخصیص در بک‌اند ثبت می‌شود و در بار بعدی بارگذاری صفحه از بین نمی‌رود.
2. **با فرض** عضوی با نقش «فقط مشاهده» به پروژه اضافه شده، **وقتی** تلاش می‌کند تسکی را ویرایش یا حذف کند، **آنگاه** سیستم (چه در UI و چه در API) درخواست را رد می‌کند.
3. **با فرض** نقش عضوی در پروژه توسط مدیرعامل تغییر یا حذف می‌شود، **وقتی** عضو دوباره وارد پروژه می‌شود، **آنگاه** دسترسی‌اش بلافاصله مطابق نقش جدید (یا عدم دسترسی) به‌روز شده است.

---

### یوزراستوری ۶ — مشاهده و انجام تسک/اسپرینت/مایلستون توسط عضو (اولویت: P2)

عضوی که به پروژه اضافه شده می‌تواند پروژه، تسک‌های تعریف‌شده، اسپرینت‌ها یا مایلستون‌ها را ببیند و آن‌ها را انجام دهد/به‌روزرسانی کند.

**چرا این اولویت**: این‌جاست که کار روزانهٔ تیم واقعاً اتفاق می‌افتد؛ ارزش عملیاتی اصلی پلتفرم برای کاربران غیرمدیرعامل.

**تست مستقل**: می‌توان با ورود یک عضو تخصیص‌یافته به پروژه، مشاهدهٔ تسک‌های او، و تغییر وضعیت یک تسک، این استوری را مستقل تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** عضو به پروژه‌ای با نقش مشخص اضافه شده، **وقتی** وارد آن پروژه می‌شود، **آنگاه** فقط تسک‌ها/اسپرینت‌ها/مایلستون‌های همان پروژه را می‌بیند، نه پروژه‌های دیگر.
2. **با فرض** تسکی به عضو تخصیص داده شده، **وقتی** وضعیت تسک را به «انجام‌شده» تغییر می‌دهد، **آنگاه** درصد پیشرفت پروژه به‌صورت خودکار به‌روزرسانی می‌شود.
3. **با فرض** عضو از پروژه حذف شده، **وقتی** دوباره تلاش می‌کند به آن پروژه دسترسی پیدا کند (مثلاً از طریق لینک مستقیم)، **آنگاه** دسترسی رد می‌شود.

---

### یوزراستوری ۷ — بایگانی پروژهٔ تکمیل‌شده و نمایش در «برد» (اولویت: P3)

پس از رسیدن پروژه به ۱۰۰٪ و تایید توسط کارفرما، پروژه به بخش «انجام‌شده‌ها» منتقل می‌شود و به‌صورت یک برد، زمان انجام و درصد موفقیت را نمایش می‌دهد.

**چرا این اولویت**: ارزش گزارش‌گیری/نمایشی دارد اما به اجرای عملیاتی پروژه (استوری‌های ۴ تا ۶) وابسته است و می‌تواند بعداً اضافه شود.

**تست مستقل**: می‌توان با رساندن یک پروژهٔ آزمایشی به ۱۰۰٪، تایید آن، و بررسی ظاهرشدنش در برد «انجام‌شده‌ها»، این استوری را مستقل تست کرد.

**سناریوهای پذیرش**:

1. **با فرض** همهٔ تسک‌های یک پروژه انجام‌شده (۱۰۰٪)، **وقتی** کارفرما تکمیل پروژه را تایید می‌کند، **آنگاه** پروژه به وضعیت «بایگانی/انجام‌شده» تغییر می‌کند و دیگر در فهرست پروژه‌های فعال نیست.
2. **با فرض** پروژه بایگانی شده، **وقتی** کاربر وارد برد «انجام‌شده‌ها» می‌شود، **آنگاه** تاریخ تکمیل، مدت‌زمان اجرا و درصد موفقیت/کیفیت پروژه نمایش داده می‌شود.
3. **با فرض** پروژه به ۱۰۰٪ رسیده اما هنوز توسط کارفرما تایید نشده، **وقتی** بررسی می‌شود، **آنگاه** پروژه هنوز در فهرست «فعال» باقی می‌ماند، نه در «انجام‌شده‌ها».

---

## موارد مرزی (Edge Cases)

- کد ملی وارد‌شده قبلاً برای حساب دیگری ثبت و تایید شده باشد چه اتفاقی می‌افتد؟
- اگر کد سازمانی لو برود (مثلاً در گروه عمومی پخش شود)، آیا افراد غیرمرتبط می‌توانند با آن به سازمان بپیوندند؟ آیا سقف نفرات یا محدودیت زمانی دارد؟
- اگر مدیرعامل حساب کاربری خودش غیرفعال/حذف شود، چه بر سر پروژه‌ها و اعضای زیرمجموعه‌اش می‌آید؟
- اگر عضوی هم‌زمان در چند پروژه با نقش‌های متفاوت باشد، تداخل دسترسی چگونه مدیریت می‌شود؟
- اگر اتصال اینترنت قطع شود درست در لحظهٔ ذخیرهٔ پروژه/تسک، آیا داده از بین می‌رود یا صف (queue) و تلاش مجدد وجود دارد؟
- نقش «کارفرما»یی که پروژه را تایید می‌کند دقیقاً همان مدیرعامل است یا یک نقش/کاربر جداگانه (مثلاً کارفرمای بیرونی که فقط ناظر است)؟ [NEEDS CLARIFICATION]
- اگر پروژه به ۱۰۰٪ برسد ولی کارفرما آن را رد کند (نه تایید)، چه وضعیتی برای پروژه ثبت می‌شود؟
- وقتی مرورگر/کش کاربر پاک شود، دادهٔ فعلاً در localStorage (پروژه‌ها) چه سرنوشتی پیدا می‌کند؟ آیا راهی برای migrate کردن دادهٔ قدیمی localStorage به بک‌اند وجود دارد؟

## نیازمندی‌ها *(الزامی)*

### نیازمندی‌های عملکردی

- **FR-001**: سیستم باید امکان ثبت‌نام مدیرعامل را فقط از طریق یک لینک اختصاصی/دعوت فراهم کند، نه ثبت‌نام آزاد عمومی.
- **FR-002**: سیستم باید کد ملی وارد‌شده توسط مدیرعامل را قبل از فعال‌سازی کامل حساب، از طریق یک فرآیند تایید (داخلی یا سرویس بیرونی) بررسی کند. [NEEDS CLARIFICATION: تایید به‌صورت خودکار از طریق استعلام سرویس دولتی/شرکتی انجام می‌شود یا تایید دستی توسط تیم پشتیبانی؟]
- **FR-003**: سیستم باید به هر مدیرعامل تایید‌شده یک کد سازمانی یکتا اختصاص دهد که برای دعوت اعضا استفاده می‌شود.
- **FR-004**: سیستم باید امکان باطل‌کردن/بازتولید کد سازمانی را برای مدیرعامل فراهم کند.
- **FR-005**: سیستم باید کاربران ثبت‌نام‌شده با یک کد سازمانی مشخص را به‌صورت زیرمجموعهٔ همان مدیرعامل ثبت کند.
- **FR-006**: سیستم باید به مدیرعامل امکان ایجاد پروژهٔ جدید را بدهد و پروژه را در **دیتابیس بک‌اند** (نه در حافظهٔ محلی مرورگر/localStorage) ذخیره کند.
- **FR-007**: سیستم باید تمام عملیات خواندن/نوشتن پروژه‌ها، تسک‌ها، اسپرینت‌ها و مایلستون‌ها را از طریق API بک‌اند انجام دهد تا داده مستقل از مرورگر/دستگاه کاربر باقی بماند.
- **FR-008**: سیستم باید به مدیرعامل امکان تخصیص نقش مشخص به هر عضو در هر پروژه را بدهد (رابطهٔ چندبه‌چند بین کاربر، پروژه و نقش).
- **FR-009**: سیستم باید دسترسی هر عضو به پروژه، تسک‌ها و عملیات (مشاهده/ویرایش/حذف) را بر اساس نقش او در همان پروژه، هم در فرانت‌اند و هم در سطح API بک‌اند اعمال و اعتبارسنجی کند (نه فقط مخفی‌کردن دکمه در UI).
- **FR-010**: سیستم باید به عضوِ تخصیص‌یافته امکان مشاهده و به‌روزرسانی وضعیت تسک‌ها/اسپرینت‌ها/مایلستون‌های پروژه‌ای که در آن عضو است را بدهد.
- **FR-011**: سیستم باید درصد پیشرفت پروژه را بر اساس وضعیت تسک‌های آن به‌صورت خودکار محاسبه کند.
- **FR-012**: سیستم باید هنگام رسیدن پروژه به ۱۰۰٪ درصد پیشرفت، امکان تایید نهایی پروژه توسط [NEEDS CLARIFICATION: نقش «کارفرما»] را فراهم کند.
- **FR-013**: سیستم باید پس از تایید نهایی، پروژه را از فهرست «پروژه‌های فعال» به بخش «پروژه‌های انجام‌شده/بایگانی» منتقل کند.
- **FR-014**: سیستم باید در بخش «انجام‌شده‌ها» برای هر پروژه، تاریخ/بازهٔ تکمیل و درصد موفقیت را به‌صورت یک آیتم برد (کارت) نمایش دهد.
- **FR-015**: سیستم باید کاربرانی که هنوز به هیچ پروژه‌ای اضافه نشده‌اند را از دیدن پروژه‌های سایر اعضای سازمان منع کند.
- **FR-016**: سیستم باید داده‌های سازمان‌های مختلف (پروژه، اعضا، کد سازمانی) را کاملاً از هم ایزوله نگه دارد (چندمستأجری/multi-tenant).

### موجودیت‌های کلیدی

- **کاربر (User)**: حساب پایه در سیستم؛ یکی از نقش‌های سیستمی «مدیرعامل» یا «عضو» را دارد؛ شامل اطلاعات هویتی و کد ملی (فقط برای مدیرعامل).
- **سازمان (Organization)**: واحدی که حول یک مدیرعامل شکل می‌گیرد؛ دارای کد سازمانی یکتا و فهرستی از اعضا.
- **عضویت پروژه (ProjectMembership)**: رابطهٔ بین یک کاربر، یک پروژه و یک نقش مشخص در آن پروژه (چندبه‌چند).
- **پروژه (Project)**: واحد اصلی کار؛ متعلق به یک سازمان؛ دارای وضعیت (فعال/تکمیل‌شده/بایگانی)، درصد پیشرفت، و تاریخ‌های شروع/پایان.
- **تسک/اسپرینت/مایلستون (Task/Sprint/Milestone)**: واحدهای کاری زیرمجموعهٔ یک پروژه که به اعضا تخصیص می‌یابند.
- **کد سازمانی (InvitationCode)**: کد یکتای مرتبط با یک سازمان برای عضویت اعضای جدید.
- **رکورد تایید هویت (IdentityVerificationRequest)**: رکورد وضعیت تایید کد ملی مدیرعامل (در انتظار/تایید‌شده/رد‌شده).
- **رکورد پروژهٔ بایگانی‌شده (CompletedProjectRecord)**: خلاصهٔ یک پروژهٔ تکمیل و تایید‌شده برای نمایش در برد «انجام‌شده‌ها».

## معیارهای موفقیت *(الزامی)*

### نتایج قابل‌اندازه‌گیری

- **SC-001**: پس از پاک‌شدن کش مرورگر یا ورود از دستگاه دیگر، ۱۰۰٪ پروژه‌های ایجادشده همچنان در دسترس‌اند (یعنی هیچ دادهٔ پروژه‌ای صرفاً در localStorage باقی نمی‌ماند).
- **SC-002**: مدیرعامل می‌تواند فرآیند ساخت پروژه و تخصیص حداقل یک عضو با نقش مشخص را در کمتر از ۳ دقیقه کامل کند.
- **SC-003**: هیچ عضوی نمی‌تواند پروژه یا تسکی خارج از پروژه‌های تخصیص‌یافته به خودش را مشاهده یا ویرایش کند (۰ نمونهٔ نقض دسترسی در تست امنیتی).
- **SC-004**: درصد پیشرفت نمایش‌داده‌شده برای هر پروژه با وضعیت واقعی تسک‌های آن ۱۰۰٪ همخوان است (بدون نیاز به محاسبهٔ دستی).
- **SC-005**: زمان بین تایید نهایی یک پروژه توسط کارفرما و ظاهرشدنش در برد «انجام‌شده‌ها» کمتر از چند ثانیه است (بلادرنگ/near real-time).

## فرضیات (Assumptions)

- فرض شده هر کاربر عضو دقیقاً یک سازمان (زیرمجموعهٔ یک مدیرعامل) است؛ عضویت هم‌زمان در چند سازمان خارج از محدودهٔ این نسخه است.
- فرض شده «کارفرما»یی که پروژه را تایید می‌کند همان مدیرعامل سازمان است، مگر آنکه در جلسهٔ `/speckit.clarify` خلاف آن مشخص شود.
- فرض شده بک‌اند/دیتابیس موجود (که طبق گفتهٔ کاربر وجود دارد ولی هنوز به فرانت‌اند برای پروژه‌ها متصل نیست) قابلیت افزودن جدول‌ها/Endpoint های جدید برای Project، ProjectMembership، Task، Sprint، Milestone و InvitationCode را دارد.
- فرض شده احراز هویت (login/session) از قبل در پلتفرم پیاده‌سازی شده و این مشخصات فقط لایهٔ سازمان/پروژه/نقش را روی آن اضافه می‌کند.
- فرض شده کد سازمانی به‌تنهایی کافی برای عضویت است و مرحلهٔ تاییدِ دستیِ اضافی توسط مدیرعامل برای هر عضو جدید لازم نیست، مگر خلاف آن گفته شود.
